# cc-kit — persisted API keys
#
# Written by `cc-switch` (mode 0600). Each provider's key is its own
# uppercase variable. Do NOT commit this file — it is in .gitignore.
#
# To replace a key, run:  cc-switch <provider> --new-key

export DEEPSEEK_API_KEY=""
export MINIMAX_API_KEY=""
export ZHIPU_API_KEY=""
export ANTHROPIC_API_KEY=""
